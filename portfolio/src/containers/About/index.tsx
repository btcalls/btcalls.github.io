import React from 'react';

import BasePage from 'components/BasePage';

export default function About(): React.ReactElement {
  /**
   * Sections:
   * - Currently doing
   * - Most passionate in terms of work
   * - Accomplishments
   *
   * Less than 250 words
   * With images
   */
  return (
    <BasePage>
      <span>About Me</span>
    </BasePage>
  );
}
